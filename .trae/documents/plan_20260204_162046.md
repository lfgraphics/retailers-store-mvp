I will now execute the confirmed plan.

### **Phase 1: Configuration & Cleanup**
1.  **Update `next.config.ts`**: Change `sw` to `'service-worker.js'`.
2.  **Cleanup**: Delete `public/sw-push.js`.

### **Phase 2: Database Schema & Seeding**
3.  **Create `src/models/Admin.ts`**: Define the new Admin schema.
4.  **Update `src/models/Retailer.ts`**: Add `defaultDeliveryCharge`.
5.  **Update `src/models/Coupon.ts`**: Add `isFreeDelivery`.
6.  **Update `src/lib/seed-service.ts`**: Implement the split seeding logic (Admin + Store).

### **Phase 3: Backend Logic**
7.  **Auth API**: Update `app/api/auth/retailer/login/route.ts` to use `Admin` model.
8.  **Order API**: Update `app/api/orders/route.ts` for multiple coupons and delivery charges.
9.  **Settings API**: Update `app/api/retailer/settings/route.ts` to handle delivery charge updates.

### **Phase 4: Frontend Implementation**
10. **Checkout**: Update `app/checkout/page.tsx` UI for coupons and delivery fee.
11. **Settings**: Update `app/retailer/profile/page.tsx` to manage delivery charge.
12. **Admins**: Create `app/retailer/admins/page.tsx` for admin management.
